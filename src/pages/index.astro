---
import '../styles/global.css'
import Layout from '@/layouts/Layout.astro'
---
<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>



<Layout content={{ title: "Astro Blog" }}>
<div class="text-center flex flex-col items-center justify-center min-h-screen ">
  <h1 class="text-4xl font-bold mb-4">Welcome to My Astro Blog</h1>
  <p class="text-lg font-medium ">This is my first project with Astro. Explore the site to learn more!</p>
<img src="/public/animations/rocket.gif" alt="Rocket Animation" class="w-64 h-auto mt-6" />

</div>

</Layout>

